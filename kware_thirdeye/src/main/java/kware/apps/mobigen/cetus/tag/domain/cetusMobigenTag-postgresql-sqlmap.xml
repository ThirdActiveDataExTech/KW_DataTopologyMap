<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cetusMobigenTag">

    <insert id="insert" parameterType="CetusMobigenTag" useGeneratedKeys="true" keyProperty="uid">
        /* cetusMobigenTag.insert */
        INSERT INTO CETUS_MOBIGEN_TAG (
             TAG_NM
        ) VALUES (
             #{tagNm}
        )
    </insert>

    <select id="getMobigenTagList" resultType="TagList">
        /* cetusMobigenTag.getMobigenTagList */
        SELECT UID AS TAG_UID
              ,TAG_NM
          FROM CETUS_MOBIGEN_TAG
    </select>

</mapper>